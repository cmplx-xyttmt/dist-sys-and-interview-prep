# Say-list terms with no source yet (L1–L8)

For Claude: add each as a read step or an inline explanation when that rung gets its two-half script.
Found Sep 24 2026 (links checked, all returned 200).

- L1: page cache (OSTEP ch. 40, 40.7 "Caching and Buffering", file-implementation.pdf); fsync (OSTEP ch. 39, 39.7, file-intro.pdf); Python's file buffer (docs.python.org/3/library/io.html, "Buffered Streams")
- L2: cryptographic hash / SHA-256 (Wikipedia "Cryptographic hash function"); GFS (SOSP 2003 paper, 2.3–2.4)
- L3: read-through cache and invalidation (aws.amazon.com/caching/best-practices/, lazy loading); NVMe (inline; Wikipedia "NVM Express")
- L4: point-in-time restore (litestream.io/reference/restore/, -timestamp)
- L5: copy-on-write in general (Wikipedia "Copy-on-write")
- L6: service discovery (nginx.com/blog/service-discovery-in-a-microservices-architecture/)
- L7: token broker (fly.io/blog/tokenized-tokens/); Authorization: Bearer (MDN "Authorization"); OAuth (aaronparecki.com/oauth-2-simplified/)
